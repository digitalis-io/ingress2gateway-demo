kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ms784
nodes:
  - role: control-plane
    extraPortMappings:
      # Envoy NodePort 30942 -> localhost:9042 so cqlsh can run from the host
      - containerPort: 30942
        hostPort: 9042
        protocol: TCP
